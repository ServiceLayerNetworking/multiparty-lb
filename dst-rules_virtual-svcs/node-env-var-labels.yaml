
apiVersion: networking.istio.io/v1beta1
kind: ProxyConfig
metadata:
  name: proxyconfig0
  namespace: default
spec:
  selector:
    matchLabels:
      kubernetes.io/hostname: node0.${CLUSTER_NAME}
  environmentVariables:
    ISTIO_META_NODE_NAME: node0.${CLUSTER_NAME}
---
apiVersion: networking.istio.io/v1beta1
kind: ProxyConfig
metadata:
  name: proxyconfig1
  namespace: default
spec:
  selector:
    matchLabels:
      kubernetes.io/hostname: node1.${CLUSTER_NAME}
  environmentVariables:
    ISTIO_META_NODE_NAME: node1.${CLUSTER_NAME}
---
apiVersion: networking.istio.io/v1beta1
kind: ProxyConfig
metadata:
  name: proxyconfig2
  namespace: default
spec:
  selector:
    matchLabels:
      kubernetes.io/hostname: node2.${CLUSTER_NAME}
  environmentVariables:
    ISTIO_META_NODE_NAME: node2.${CLUSTER_NAME}
---
apiVersion: networking.istio.io/v1beta1
kind: ProxyConfig
metadata:
  name: proxyconfig3
  namespace: default
spec:
  selector:
    matchLabels:
      kubernetes.io/hostname: node3.${CLUSTER_NAME}
  environmentVariables:
    ISTIO_META_NODE_NAME: node3.${CLUSTER_NAME}
---
apiVersion: networking.istio.io/v1beta1
kind: ProxyConfig
metadata:
  name: proxyconfig4
  namespace: default
spec:
  selector:
    matchLabels:
      kubernetes.io/hostname: node4.${CLUSTER_NAME}
  environmentVariables:
    ISTIO_META_NODE_NAME: node4.${CLUSTER_NAME}
---
apiVersion: networking.istio.io/v1beta1
kind: ProxyConfig
metadata:
  name: proxyconfig5
  namespace: default
spec:
  selector:
    matchLabels:
      kubernetes.io/hostname: node5.${CLUSTER_NAME}
  environmentVariables:
    ISTIO_META_NODE_NAME: node5.${CLUSTER_NAME}
